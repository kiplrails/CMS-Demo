<h1>All Companies</h1>

<% @companies.each do |company| %>
  <li>
    <%= link_to company.name, admin_company_path(company) %>
    <%= link_to "Edit", edit_admin_company_path(company) %>
  </li>
<% end %>

<%= link_to "Add a company", new_admin_company_path %>